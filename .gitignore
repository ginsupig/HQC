venv/
.venv/
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# State files (runtime data, not source)
state/

# Market data (large; fetched via fetch_alpaca.py, not source)
data/alpaca/

# Research run artifacts (generated by B-tools / campaign / walk-forward).
# Anchored to repo root so committed CSVs under docs/ etc. are unaffected.
/result*.json
/*.csv

# Backups
*.backup
*.bak

# Secrets / local env
.env
*.env.*

# IDE / OS
.vscode/
.idea/
*.DS_Store
Thumbs.db
